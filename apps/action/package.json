{
  "name": "@clipcode/action",
  "version": "0.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && ncc build dist/main.js -o dist --minify",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0",
    "@actions/glob": "^0.4.0",
    "@clipcode/analyzer": "^0.0.0",
    "@clipcode/crawler": "^0.0.0",
    "@clipcode/core": "^0.0.0",
    "@clipcode/templates": "^0.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.3.3",
    "vitest": "^1.3.0"
  }
}